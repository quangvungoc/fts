<div>
<%= question.content %>
<br>
<% answers.each do |answer| %>
  <%= radio_button_tag("choices" + answer.id.to_s, 1, 
    answer_details.answer_id == answer.id, :readonly => @readonly) %> 
  <%= answer.content %> <br>
<% end %>
</div>