<%= render 'shared/error_messages', object: f.object %>

Subject:
<%= f.collection_select :subject_id, Subject.all, :id, :name %>

Type:
<% question_types = { S: "Single Choice", T: "Text", E: "Excercise"} %>
<%= f.select :question_type, question_types.map{|type| [type[1], type[0]]}, {} %>

Content:
<%= f.text_area :content, size: "25x8"%>
